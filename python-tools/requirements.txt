# Verity Systems API - Production Dependencies
# Updated January 2026

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0

# HTTP Client
# Pin httpx to a known-compatible version for Supabase client libraries.
# If you maintain Supabase client, update this pin after validating compatibility.
httpx==0.25.1
# Pin httpcore to a version compatible with httpx 0.25.1 to avoid transport
# signature mismatches (fixes AsyncConnectionPool socket_options error).
httpcore==0.17.1

# Environment
python-dotenv>=1.0.1

# Security & Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.2.0
PyJWT>=2.9.0

# Payments
stripe>=11.0.0

 # Async HTTP
 aiohttp>=3.8.0

# Postgres client for migrations
psycopg2-binary>=2.9.7

# ORM + Migrations (for production migration path)
SQLAlchemy>=2.0.0,<3.0.0
alembic>=1.12.0
cryptography>=41.0.0
celery>=5.3.0
redis>=4.6.0
# Developer / test helpers (optional for CI/integration runs)
requests>=2.31.0
websocket-client>=1.6.2
psycopg[binary]>=3.2.0
